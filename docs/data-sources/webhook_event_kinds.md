---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "iaas_webhook_event_kinds Data Source - iaas"
subcategory: ""
description: |-
  Published catalog of every event kind a webhook subscription's event_kinds may name (or "*" for all). Validate a kind against this list before referencing it in an iaas_webhook_subscription (or the API directly) instead of hardcoding the vocabulary -- an unrecognised kind is rejected with a 422 at subscribe time.
---

# iaas_webhook_event_kinds (Data Source)

Published catalog of every event kind a webhook subscription's `event_kinds` may name (or `"*"` for all). Validate a kind against this list before referencing it in an `iaas_webhook_subscription` (or the API directly) instead of hardcoding the vocabulary -- an unrecognised kind is rejected with a 422 at subscribe time.

## Example Usage

```terraform
data "iaas_webhook_event_kinds" "all" {}

output "webhook_event_kinds" {
  value = data.iaas_webhook_event_kinds.all.kinds
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Read-Only

- `kinds` (Attributes List) Every published event kind. (see [below for nested schema](#nestedatt--kinds))

<a id="nestedatt--kinds"></a>
### Nested Schema for `kinds`

Read-Only:

- `description` (String) Human-readable description of what triggers this kind.
- `kind` (String) Event kind value, e.g. `cluster.created` or `instance.deleted`.
- `resource` (String) The resource family the kind belongs to, e.g. `kubernetes_cluster` or `instance`.
